namespace Service.Handler
{
    public class FileHandler : AbstractHandler
    {
        public override byte[] perform(Candidate candidate, byte[] target)
        {
            return base.perform(candidate, target);
        }
    }
}